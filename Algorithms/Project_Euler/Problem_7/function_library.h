#pragma once

// Function prototypes:

// prime number check function
bool prime_check(int num);

// prime number list function (lists all of the prime numbers, up to a limit)
int prime_num_term(int limit);
